# Backend Portfolio

Static portfolio site for Ahmed, built from local GitHub project context. It
positions the work around Laravel backends, React/Inertia full-stack products,
Flutter mobile work, payment operations, HR workflows, payroll/personnel systems,
research, education, and integration tooling.

## Files

- `index.html` - page structure and portfolio content
- `styles.css` - responsive visual design
- `script.js` - lightweight animated backend system map

## Run locally

Open `index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 5173
```

Then visit `http://127.0.0.1:5173`.

## Before publishing

- Add a resume download link if desired.
- Keep project descriptions honest and update private repo links if public viewers cannot access them.
